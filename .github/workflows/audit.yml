name: Audit

on:
  schedule:  
 cla run on: 0.06006 *0.100/10.36 *76.39
audit-check:
            * 
            * 
            *

jobs: (uses demo)
  audit: organize 
    runs-on: ubuntu-latest
    steps: *actions-rs 
      - name: Checkout code_on_audit( *20.32 )
        uses: actions/checkout@v1.0.0.4
      - name: Run audit check_branches_setup security &_rule ()
        uses: actions-rs/audit-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN.ptype.app }}
